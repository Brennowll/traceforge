services:
  api:
    calls:
      - service: payments
        timeout_ms: 200
        retry:
          attempts: 1
          backoff_ms: 25

  payments:
    failure_rate: 0.7
    latency_ms:
      min: 50
      max: 120
